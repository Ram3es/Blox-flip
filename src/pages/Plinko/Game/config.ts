const pins = {
  startPins: 3,
  pinSize: 6,
  pinGap: 40
}

const ball = {
  ballSize: 5.7
}

const engine = {
  engineGravity: 1.0
}

const world = {
  width: 700,
  height: 700
}

export const config = {
  pins,
  ball,
  engine,
  world
}
