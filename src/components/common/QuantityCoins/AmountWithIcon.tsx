import React, { FC, ReactNode } from 'react'
interface IProps {
  children: ReactNode
}

const AmountWithIcon: FC<IProps> = ({ children }) => {
  return (
        <div>

        </div>
  )
}

export default AmountWithIcon
